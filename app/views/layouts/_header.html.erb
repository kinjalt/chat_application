<% if user_signed_in? %>
  <span class="mt-2">Welcome <%= current_user.name %>!!</span>
  <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-primary float-right mt-2" %>
<% end %>